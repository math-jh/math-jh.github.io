---
title: "Divisors"
description: "We introduce the notion of divisors as a systematic way to encode the zeros and poles of rational functions on a variety, and examine the definitions of Weil divisors and principal divisors."
excerpt: "Weil divisors, Cartier divisors, and divisor class groups"

categories: [Math / Algebraic Varieties]
permalink: /en/math/algebraic_varieties/divisors
sidebar:
    nav: "algebraic_varieties-en"

date: 2026-03-25
weight: 9
translated_at: 2026-08-17T10:46:41+00:00
translation_source: kimi-cli
---
Consider a rational function $f \in \mathbb{K}(X)$ defined on a variety $X$. This function has zeros at some points and is undefined at others. Essentially, rational functions are genuinely of the form of rational expressions, so the points where this function is undefined are a kind of pole, and we can also consider the order of that pole. To describe this information systematically, we introduce the notion of a *divisor*.

For this to work, for each codimension 1 irreducible subvariety $Y$ of $X$, the stalk $\mathcal{O}_{X, \eta_Y}$ at its generic point $\eta_Y$ must be a discrete valuation ring. For this to be well-defined, it suffices that the local rings of $X$ are normal domains ([[Commutative Algebra] §Integral Extensions, ⁋Definition 3](/en/math/commutative_algebra/integral_extension#def3)), or more generally that they satisfy the $R_1$ condition of [[Commutative Algebra] §Regular Local Rings, ⁋Theorem 10](/en/math/commutative_algebra/regular_local_rings#thm10). However, a variety being normal is not such a difficult condition, and if a variety is not normal, we can avoid this issue by considering the normalization of [[Commutative Algebra] §Regular Local Rings, ⁋Definition 9](/en/math/commutative_algebra/regular_local_rings#def9), so in contexts where divisors appear, we implicitly understand $X$ to be normal.

## Weil Divisors

We first examine the most intuitive definition of a divisor, the *Weil divisor*. This is a formal sum of codimension 1 closed subvarieties with integer coefficients, where the integer coefficient of each term represents the order of a zero or pole at the corresponding closed subvariety.

::: Definition 1
A *Weil divisor* on a variety $X$ is a formal $\mathbb{Z}$-linear combination of codimension 1 (irreducible) closed subvarieties of $X$:

$$D = \sum_{i=1}^{r} n_i Y_i$$

We denote the set of Weil divisors by $\Div(X)$.
:::

Then $\Div(X)$ forms an abelian group under addition.

::: Definition 2
A Weil divisor $D = \sum_i n_i Y_i$ is *effective* if all $n_i \ge 0$. We denote this by $D \ge 0$.
:::

Meanwhile, as mentioned above, the basic idea of a Weil divisor is to encode information about the zeros and poles of a rational function $f \in \mathbb{K}(X)^\times$. This is called a *principal divisor*.

::: Definition 3
The *principal divisor* $\divisor(f)$ of a rational function $f \in \mathbb{K}(X)^\times$ is defined as follows:

$$\divisor(f) = \sum_{Y} v_Y(f) \cdot Y$$

where the sum is over all codimension 1 irreducible closed subvarieties $Y$ of $X$, and $v_Y(f)$ is the integer representing the order of the zero or pole of $f$ along $Y$.
:::

Since $X$ is Noetherian, there are only finitely many $Y$ with $v_Y(f)\neq 0$, so the above sum is finite and defines a Weil divisor as in [Definition 1](#def1).

If $Y$ is a codimension 1 closed subvariety and $X$ is smooth at a point $x \in Y$, then near $x$, $Y$ is defined by a single regular function $\pi$. This $\pi$ is called a *local equation* near $x$. Now expand the rational function $f$ near $x$ as $f = \pi^{v_Y(f)} \cdot u$. Here, all information about the zeros and poles of $f$ is contained in $\pi^{v_Y(f)}$, so $u$ is a function with neither zeros nor poles near $x$, and $v_Y(f)$ is the zero/pole order along $Y$.

To make this mathematically rigorous, the stalk $\mathcal{O}_{X, \eta}$ of the structure sheaf at the generic point $\eta$ of $Y$ becomes a discrete valuation ring, and the local equation $\pi$ corresponds to a uniformizer of this discrete valuation ring. Then $v_Y(f)$ is exactly the valuation of this discrete valuation ring. For this to make sense, the stalk $\mathcal{O}_{X, \eta}$ at the generic point of $Y$ must be a discrete valuation ring, and this is precisely the reason we take $X$ to be a normal variety.

::: Example 4
Consider the regular function $f(\x, \y) = \x^2 \y$ defined on $\mathbb{A}^2$. This function has no poles, and zeros only along the two irreducible closed subvarieties $D_1=Z(\x)$ and $D_2=Z(\y)$. Here, the zero along $D_1$ has order 2, and the zero along $D_2$ has order 1, so the principal divisor corresponding to $f$ is

$$\divisor(f)=2D_1+D_2$$
:::

::: Example 5
Consider the rational function on $\mathbb{A}^1$

$$g(\x) = \frac{(\x-a_1)^{n_1} \cdots (\x-a_k)^{n_k}}{(\x-b_1)^{m_1} \cdots (\x-b_l)^{m_l}}$$

This function has zeros of order $n_i$ along the $a_i$ and poles of order $m_j$ along the $b_j$, so the principal divisor of this function is

$$\divisor(g) = n_1(a_1) + \cdots + n_k(a_k) - m_1(b_1) - \cdots - m_l(b_l)$$

Here $(a_i)$ denotes the divisor of the point $a_i$, where positive coefficients mean zeros and negative coefficients mean poles.
:::

::: Proposition 6
$\divisor: \mathbb{K}(X)^\times \rightarrow \Div(X)$ is a group homomorphism.
:::
::: Proof
For $f, g \in \mathbb{K}(X)^\times$, we have for each $Y$

$$v_Y(fg) = v_Y(f) + v_Y(g)$$

so

$$\divisor(fg) = \sum_Y v_Y(fg) \cdot Y = \sum_Y (v_Y(f) + v_Y(g)) \cdot Y = \divisor(f) + \divisor(g)$$
:::

Our goal is to extract properties of $X$ from $\Div(X)$. However, $\Div(X)$ is unnecessarily large for this purpose. Since we already know the elements of $\mathbb{K}(X)^\times$ reasonably well, we will treat a divisor obtained from another divisor by adding a divisor coming from an element of $\mathbb{K}(X)^\times$ as equivalent to the original divisor. That is, we define the following.

::: Definition 7
Two Weil divisors $D_1, D_2$ are *linearly equivalent* if there exists a rational function $f \in \mathbb{K}(X)^\times$ such that $D_1 - D_2 = \divisor(f)$. We denote this by $D_1 \sim D_2$.
:::

Then the following holds.

::: Proposition 8
Linear equivalence $\sim$ is an equivalence relation on $\Div(X)$.
:::

::: Proof
First, $D - D = 0 = \divisor(1)$, so $D \sim D$. Also, if $D_1 \sim D_2$, then there exists $f$ with $D_1 - D_2 = \divisor(f)$, and for this $f$ we have $D_2 - D_1 = \divisor(f^{-1})$. Finally, assume $D_1 \sim D_2$ and $D_2 \sim D_3$. That is, there exist $f, g$ with $D_1 - D_2 = \divisor(f)$ and $D_2 - D_3 = \divisor(g)$. Then $D_1 - D_3 = \divisor(fg)$, so $D_1 \sim D_3$.
:::

::: Definition 9
We define the *divisor class group* $\Cl(X)$ of $X$ as the quotient group of $\Div(X)$ by linear equivalence:

$$\Cl(X) = \Div(X) / \{\divisor(f) \mid f \in \mathbb{K}(X)^\times\}$$
:::

To know which elements of $\Div(X)$ are the same in $\Cl(X)$, it suffices to examine which divisor is linearly equivalent to $0$. This is essentially the algebraic geometric translation of [[Algebraic Topology] §Homotopy, ⁋Definition 2](/en/math/algebraic_topology/homotopy#def2). Two continuous functions $f_0, f_1:X \rightarrow Y$ are *homotopic* if there exists a continuous function

$$H:X\times[0,1]\rightarrow Y$$

satisfying (i) $H(x, 0) = f_0(x)$, (ii) $H(x, 1) = f_1(x)$. That is, as $t$ moves from $0$ to $1$, the family of functions $(H(-,t))_t$ continuously deforms $f_0$ into $f_1$, and homotopy takes the existence of such a family connecting two objects as an equivalence relation. To translate this concept into algebraic geometry, we use $\mathbb{P}^1$ as the parameter space instead of $[0,1]$. More concretely, viewing an element $f$ of $\mathbb{K}(X)^\times$ as a rational map $X \dashrightarrow \mathbb{P}^1$, we can consider the closure of the graph over the open set $U\subseteq X$ where $f$ is defined:

$$\Gamma_f =\overline{\{(x, t)\in U\times \mathbb{P}^1\mid f(x)=t\}}\subseteq X \times \mathbb{P}^1$$

Then pulling back the coordinate of $\mathbb{P}^1$ via the canonical projection $\pr_2:\Gamma_f\rightarrow \mathbb{P}^1$ gives $f$ itself, and thus $\divisor(f)$ measures, from the perspective of $\Gamma_f$, the difference between the $t=0$ section and the $t=\infty$ section.

That is, from this motivation, we can say that $\Cl(X)$ is smaller than $\Div(X)$ while still containing information about the properties of $X$.

::: Example 10
$\Cl(\mathbb{A}^n) = 0$. To verify this, suppose an arbitrary Weil divisor $D=\sum n_i Y_i$ is given. We must show that this is the principal divisor of some function. Since each $Y_i$ is an irreducible closed subvariety, it corresponds to a prime ideal $I(Y_i)$ of the coordinate ring $\mathbb{K}[\x_1,\ldots, \x_n]$, and $\codim Y_i=\codim I(Y_i)=1$. But $\mathbb{K}[\x_1,\ldots, \x_n]$ is a unique factorization domain by [[Ring Theory] §Polynomial Rings, ⁋Theorem 16](/en/math/ring_theory/polynomial_rings#thm16), so any height 1 prime ideal must be principal. That is, there exists a function $f_i\in \mathbb{K}[\x_1,\ldots, \x_n]$ such that $I(Y_i)=(f_i)$, and from this each $Y_i$ is $Z(f_i)$. Since $f_i$ is a prime element, $\divisor(f_i)=Y_i$, and therefore $D=\divisor(\prod_i f_i^{n_i})$ is a principal divisor.
:::

Of course, to understand what kind of information $\Cl(X)$ carries, we should consider cases where $\Cl(X)\neq 0$.

::: Example 11
$\Cl(\mathbb{P}^n) \cong \mathbb{Z}$. To verify this, let us fix a hyperplane class, say $H=Z(\x_0)$, as a reference. We will first show that a hypersurface $Z(F)$ defined by an arbitrary homogeneous polynomial $F$ of degree $d$ is linearly equivalent to $dH$. To see this, consider the function $F/\x_0^d\in \mathbb{K}(\mathbb{P}^n)^\times$; then

$$\divisor(F/\x_0^d)=\divisor(F)-d\cdot \divisor(\x_0)=Z(F)-dH$$

Thus a Weil divisor $D=\sum n_i Y_i$ is determined by the orders $n_i$ and the degrees $d_i$ of the homogeneous polynomials defining the $Y_i$. Motivated by this, let us define the following map:

$$\deg: \Cl(\mathbb{P}^n) \rightarrow \mathbb{Z};\qquad D=\sum n_i Y_i \mapsto \sum n_i \deg(Y_i)$$

where $\deg(Y_i)$ is the degree of the homogeneous polynomial defining $Y_i$. Since every hypersurface is the zero set of some homogeneous polynomial, there is no problem with this definition. Also, by [[Rational Maps] §Rational Maps, ⁋Example 4](/en/math/algebraic_varieties/rational_maps#ex4), we know that any rational function on $\mathbb{P}^n$ is represented as a ratio $F/G$ of homogeneous polynomials of the same degree, so for any principal divisor $\divisor(F)$ we necessarily have $\deg(\divisor(F))=0$, and thus this definition is well-defined.

Our claim is that $\deg$ is an isomorphism. First, that $\deg$ is surjective is clear since the image of $dH$ is $d$. For injectivity, we must show that any $D$ satisfying $\deg(D)=0$ is a principal divisor. But we saw earlier that $D \sim dH$, and for this to map to $0$ under the $\deg$ map, we must have $d=0$. That is, $D$ is linearly equivalent to $0$.

Intuitively, the reason this differs from $\mathbb{A}^n$ is that global regular functions on $\mathbb{P}^n$ are constant functions. That is, if a function defined on $\mathbb{P}^n$ has a zero at some point, then it must necessarily have a pole at another point, and the sum of the orders of the zeros must equal the sum of the orders of the poles. The fact that $\Cl(\mathbb{P}^n)\cong \mathbb{Z}$ means that the $dH$ with $d\neq 0$ are essentially all non-principal divisors on $\mathbb{P}^n$, and the remaining divisors $\sum n_i D_i$ are merely distributing this order $d$ to the individual $D_i$ via a rational function $f$.
:::

## Cartier Divisors

Weil divisors are geometrically intuitive, but they have the problem of not working well on singular varieties. For example, on the cone $X=Z(\x^2 + \y^2 - \z^2) \subseteq \mathbb{A}^3$, imagine defining a principal divisor following the explanation after [Definition 3](#def3). At a smooth point of this cone, say $(1,0,1)$, considering a codimension 1 subvariety such as $X\cap Z(\y)$, this subvariety is defined by the local equation $\y=0$ near this point, so for any given rational function $f$ we can extract zero and pole information with respect to $\y$ here. The problem occurs at the singular point. Consider, for example, the codimension 1 subvariety $L=(t,0,t)$ passing through the origin $(0,0,0)$. To represent this, both equations $\y=0$ and $\x-\z=0$ are needed. Indeed, since $(\x-\z)(\x+\z)=-\y^2$ holds on $X$, we have $\divisor(\x-\z)=2L$, and the ideal $(\y,\x-\z)$ of $L$ is not principal in $\mathcal{O}_{X,(0,0,0)}$, so there does not exist a single equation defining $L$ near the origin.

That is, the core problem is that on a singular variety, there can be codimension 1 subvarieties that are not expressed locally by a single equation. Therefore, we simply restrict our attention to those objects that are locally expressed by a single equation. As always, we define this via an appropriate kind of gluing.

::: Definition 12
A *Cartier divisor* on a variety $X$ is the following data:

$$\{(U_i, f_i)\}_{i \in I}$$

where $\{U_i\}$ is an open cover of $X$, each $f_i \in \mathbb{K}(X)^\times$ is a nonzero rational function, and for all $i, j$, the ratio $f_i/f_j$ is regular and non-vanishing (hence invertible) on $U_i \cap U_j$.

In this case, two data $\{(U_i, f_i)\}$ and $\{(V_j, g_j)\}$ are considered to represent the same Cartier divisor if there exists a refinement $\{W_k\}$ of the two open covers such that $f_i/g_j$ is regular and non-vanishing on it.
:::

By definition, a Cartier divisor collects only those divisors that are given locally by a single equation, i.e., *locally principal* divisors. The data $U_i$ explicitly specifies exactly which open set we are localizing to, and which single function defines it on that open set. According to this, the line $L$ examined earlier is not a Cartier divisor, since it cannot be represented by a single equation on any open neighborhood of the origin.


::: Example 13
In the example of the cone above, the line $L'=(t,0,-t)$ is, for the same reason as $L$, not a Cartier divisor. However, their sum $L+L'$ is a Cartier divisor, because $L+L'$ is defined by the zero set of $\y$.
:::

As mentioned above, a Cartier divisor can be thought of as a divisor with the additional locally principal condition. Concretely, if a Cartier divisor $\{(U_i, f_i)\}$ is given, we can consider the principal Weil divisor $\divisor(f_i)$ of $f_i$ on each $U_i$. Since $f_i/f_j$ is invertible on $U_i \cap U_j$, for all codimension 1 subvarieties $Y$ meeting $U_i \cap U_j$ we have $v_Y(f_i)=v_Y(f_j)$, and thus we can glue these together to define a Weil divisor.

We saw above that the converse does not generally hold, but at the same time we can see that there is no problem in the smooth case, since the stalk will be a regular local ring. That is, the following holds.

::: Proposition 14
On a smooth variety $X$, Weil divisors and Cartier divisors naturally correspond bijectively, and this correspondence is a group isomorphism preserving addition of divisors and sending principal Cartier divisors to principal Weil divisors.
:::

Essentially, a Weil divisor plays a role analogous to a homology class, being certain subsets of the space $X$, while a Cartier divisor, by its definition, consists of functions defined (locally) on the space $X$, playing a role analogous to a cohomology class. From this perspective, this proposition can be thought of as a kind of Poincaré duality. ([[Algebraic Topology] §Poincaré Duality, ⁋Theorem 11](/en/math/algebraic_topology/Poincare_duality#thm11))

Now we also define linear equivalence and the divisor class group for Cartier divisors, just as we did for Weil divisors. To do so, we first need the following.

::: Definition 15
For a rational function $f \in \mathbb{K}(X)^\times$, the *principal Cartier divisor* $\divisor(f)$ is defined as $\{(X, f)\}$.
:::

Then the following definition is the Cartier version of [Definition 7](#def7).

::: Definition 16
Two Cartier divisors $D_1, D_2$ are *linearly equivalent* if $D_1 - D_2$ is a principal Cartier divisor.
:::

Let us denote the group of Cartier divisors by $\CaDiv(X)$, and the subgroup generated by principal divisors by $\Prin(X)$. Then the *Cartier divisor class group* is

$$\CaCl(X) = \CaDiv(X) / \Prin(X)$$

By [Proposition 14](#prop14), on a smooth variety we have $\CaCl(X) \cong \Cl(X)$.

---

**References**

**[Hart]** R. Hartshorne, *Algebraic Geometry*, Graduate Texts in Mathematics, Springer, 1977.  
**[Sha]** I. R. Shafarevich, *Basic Algebraic Geometry I: Varieties in Projective Space*, Springer, 2013.
