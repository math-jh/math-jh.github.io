\documentclass[border=2pt]{standalone}
\usepackage{amsmath, amsthm, amsfonts}
\usepackage{tikz-cd, kotex}
\usepackage{pgfplots}
\usepgfplotslibrary{colorbrewer}
\usepackage{quiver}
\usepackage{Operators}
\usepackage{palette}
\usepackage{diagram-style}
\pgfplotsset{colormap={brass}{color=(white) color=(accent1)}, colormap={bronze}{color=(white) color=(accent2)}}
\begin{document}

%%FIG Orientation_cover_of_S1 (Poincare_Duality-1.svg)
\begin{tikzpicture}
   \begin{axis}[
 view={-20}{-20},
 zmax=80,
  xmax=2,
   ymax=2,
 height=12cm,
 clip=false,hide axis
]
\addplot3+[domain=0.7:1,samples=3000,samples y=0,accent2,no marks,thick,{stealth}-] 
({sin(deg(1.3))*x}, 
{cos(deg(1.3))*x}, 
-50);
\addplot3+[domain=1:1.3,samples=3000,samples y=0,accent1,no marks,thick,-{stealth}] 
({sin(deg(1.3))*x}, 
{cos(deg(1.3))*x}, 
-50);
\addplot3+[domain=0:2*pi,samples=500,samples y=0,black,no marks,very thick] 
({sin(deg(x))}, 
{cos(deg(x))}, 
-50);
\addplot3+[domain=0.5:1,samples=3000,samples y=0,accent3,no marks,ultra thick,{Parenthesis}-{Parenthesis}] 
({sin(deg(x))}, 
{cos(deg(x))}, 
-50);
\addplot3+[domain=0.5:1,samples=3000,samples y=0,accent2,no marks,ultra thick,{Parenthesis}-{Parenthesis}] 
({sin(deg(x))}, 
{cos(deg(x))}, 
0);
\addplot3+[domain=0.5:1,samples=3000,samples y=0,accent1,no marks,ultra thick, solid,{Parenthesis}-{Parenthesis}] 
({sin(deg(x))}, 
{cos(deg(x))}, 
20);
\end{axis}
\draw (10,6) node{$\Spe(\omega_{S^1})$};
\draw (10,3) node{$S^1$};
\draw[->] (10,5.5) -- (10,3.5) node[midway, right]{$p$};
\draw (7,1.7) node[accent3]{$U$};
\draw (7,5.3) node[accent1]{$U^+$};
\draw (7,4.3) node[accent2]{$U^-$};
\end{tikzpicture}

%%FIG Orientation_cover_of_S1_glued (Poincare_Duality-2.svg)
\begin{tikzpicture}
   \begin{axis}[
 view={-20}{-20},
 zmax=80,
  xmax=2,
   ymax=2,
 height=12cm,
 clip=false,hide axis
]
\addplot3+[domain=0.7:1,samples=100,samples y=0,accent2,no marks,thick,{stealth}-] 
({sin(deg(1.3))*x}, 
{cos(deg(1.3))*x}, 
-50);
\addplot3+[domain=1:1.3,samples=100,samples y=0,accent1,no marks,thick,-{stealth}] 
({sin(deg(1.3))*x}, 
{cos(deg(1.3))*x}, 
-50);
\addplot3+[domain=0:2*pi,samples=500,samples y=0,black,no marks,very thick] 
({sin(deg(x))}, 
{cos(deg(x))}, 
-50);
\addplot3+[domain=-1.7:2.4,samples=100,samples y=0,accent2,no marks,ultra thick] 
({sin(deg(x))}, 
{cos(deg(x))}, 
0);
\addplot3+[domain=0:2*pi,samples=100,samples y=0,accent1,no marks,ultra thick] 
({sin(deg(x))}, 
{cos(deg(x))}, 
20);
\addplot3+[domain=2.65:4.3,samples=100,samples y=0,accent2,no marks,ultra thick,solid] 
({sin(deg(x))}, 
{cos(deg(x))}, 
0);
\end{axis}
\draw (10,6) node{$\Spe(\omega_{S^1})$};
\draw (10,3) node{$S^1$};
\draw[->] (10,5.5) -- (10,3.5) node[midway, right]{$p$};
\end{tikzpicture}

%%FIG orientation_cover_of_M (Poincare_Duality-3.svg)
\begin{tikzpicture}
 \begin{scope}[xshift=3cm, yshift=-1.5cm, scale=.3]
    \begin{axis}[
    hide axis,
    view = {40}{40},
    colormap name=bronze
    ]
    \addplot3 [
    surf,
    shader     = faceted interp,opacity = 0.7,
    %shader = interp,
    point meta = x,
    samples    = 80,
    samples y  = 4,
    z buffer   = sort,
    domain     = -50:10,
    y domain   =-0.5:0.5
    ] (
    {(1+0.5*y*cos(x/2)))*cos(x)},
    {(1+0.5*y*cos(x/2)))*sin(x)},
    {0.5*y*sin(x/2)}
    );
    \addplot3 [color=accent2,thick,    
    domain     = -50:10,samples y=0,samples=100,
    ] (
    {(1+0.5*0.5*cos(x/2)))*cos(x)},
    {(1+0.5*0.5*cos(x/2)))*sin(x)},
    {0.5*0.5*sin(x/2)}
    );
    \addplot3 [color=accent2,thick,    
    domain     = 310:370,samples y=0,samples=100,
    ] (
    {(1+0.5*0.5*cos(x/2)))*cos(x)},
    {(1+0.5*0.5*cos(x/2)))*sin(x)},
    {0.5*0.5*sin(x/2)}
    );
    \end{axis}
    \end{scope}
    \begin{scope}[xshift=3cm, yshift=0cm, scale=.3]
    \begin{axis}[
    hide axis,
    view = {40}{40},
    colormap name=brass
    ]
    \addplot3 [
    surf,
    shader     = faceted interp,opacity = 0.7,
    %shader = interp,
    point meta = x,
    samples    = 80,
    samples y  = 4,
    z buffer   = sort,
    domain     = -50:10,
    y domain   =-0.5:0.5
    ] (
    {(1+0.5*y*cos(x/2)))*cos(x)},
    {(1+0.5*y*cos(x/2)))*sin(x)},
    {0.5*y*sin(x/2)}
    );
    \addplot3 [color=accent1,thick,    
    domain     = -50:10,samples y=0,samples=100,
    ] (
    {(1+0.5*0.5*cos(x/2)))*cos(x)},
    {(1+0.5*0.5*cos(x/2)))*sin(x)},
    {0.5*0.5*sin(x/2)}
    );
    \addplot3 [color=accent1,thick,    
    domain     = 310:370,samples y=0,samples=100,
    ] (
    {(1+0.5*0.5*cos(x/2)))*cos(x)},
    {(1+0.5*0.5*cos(x/2)))*sin(x)},
    {0.5*0.5*sin(x/2)}
    );
    \end{axis}
    \end{scope}
    \begin{scope}[yshift=-7cm]
    \begin{axis}[
    hide axis,
    view = {40}{40},
    colormap/YlGn
    ]
    \addplot3 [color=green!10,thick,    
    domain     = 360:720,samples y=0, samples=80,
    ] (
    {(1+0.5*0.5*cos(x/2)))*cos(x)},
    {(1+0.5*0.5*cos(x/2)))*sin(x)},
    {0.5*0.5*sin(x/2)}
    );
    \addplot3 [color=accent1,thick,    
    domain     = 0:-.1,samples y=0,samples=100,-{stealth}
    ] (
    {cos(30)+x},
    {sin(-30)+x},
    {x}
    );
    \addplot3 [
    surf,
    shader     = faceted interp,opacity = 0.1,
    %shader = interp,
    point meta = x,
    samples    = 80,
    samples y  = 4,
    z buffer   = sort,
    domain     = 0:360,
    y domain   =-0.5:0.5
    ] (
    {(1+0.5*y*cos(x/2)))*cos(x)},
    {(1+0.5*y*cos(x/2)))*sin(x)},
    {0.5*y*sin(x/2)}
    );
    \addplot3 [
    surf,
    shader     = faceted interp,opacity = 0.7,
    %shader = interp,
    point meta = x,
    samples    = 80,
    samples y  = 4,
    z buffer   = sort,
    domain     = -40:10,
    y domain   =-0.5:0.5
    ] (
    {(1+0.5*y*cos(x/2)))*cos(x)},
    {(1+0.5*y*cos(x/2)))*sin(x)},
    {0.5*y*sin(x/2)}
    );
    \addplot3 [color=green!20,thick,    
    domain     = -140:497.5,samples y=0,samples=100,
    ] (
    {(1+0.5*0.5*cos(x/2)))*cos(x)},
    {(1+0.5*0.5*cos(x/2)))*sin(x)},
    {0.5*0.5*sin(x/2)}
    );
    \addplot3 [color=accent2,thick,    
    domain     = 0:.1,samples y=0,samples=100,-{stealth}
    ] (
    {cos(30)+x},
    {sin(-30)+x},
    {x}
    );
    \end{axis}
    \end{scope}
    \draw (7,0) node{$\Spe(\omega_M)$};
    \draw (7,-5) node{$M$};
    \draw[->] (7,-.5) -- (7,-4.5);
    \draw (7,-2.5) node[right]{$p$};
\end{tikzpicture}

%%FIG MVseq_duality (Poincare_Duality-4.svg)
\begin{tikzcd}
  \cdots & {H^p_c(U\cap V)} & {H^p_c(U)\oplus H_c^p(V)} & {H^p(M)} & \cdots \\
  \cdots & {H_{n-p}(U\cap V)} & {H_{n-p}(U)\oplus H_{n-p}(V)} & {H_{n-p}(M)} & \cdots
  \arrow[from=1-1, to=1-2]
  \arrow[from=1-2, to=1-3]
  \arrow["{D_{U\cap V}}", from=1-2, to=2-2]
  \arrow[from=1-3, to=1-4]
  \arrow["{D_U\oplus D_V}", from=1-3, to=2-3]
  \arrow[from=1-4, to=1-5]
  \arrow["{D_M}", from=1-4, to=2-4]
  \arrow[from=2-1, to=2-2]
  \arrow[from=2-2, to=2-3]
  \arrow[from=2-3, to=2-4]
  \arrow[from=2-4, to=2-5]
\end{tikzcd}

%%FIG Torus_intersection (Poincare_Duality-5.svg)
\begin{tikzpicture}
  \begin{axis}[view={180}{50}, axis lines=none, colormap/YlGn]
    % Draw the torus surface
    \addplot3[
      surf,
      shader=faceted interp, 
      samples=40,
      domain=0:360,
      y domain=0:360,
      z buffer=sort,
      opacity=.8
    ]
    (
      { (3 + cos(x)) * cos(y) },
      { (3 + cos(x)) * sin(y) },
      { sin(x) }
    );
    \addplot3+ [accent1, thick,
      domain=0:180,
        samples y=0,
        mark=none
    ]
    (
      { 4 * cos(x) },
      { 4 * sin(x) },
      { 0 }
    );
    \addplot3+ [accent1, thick,
      domain=0:110,
        samples y=0,
        mark=none,
        -{stealth}
    ]
    (
      { 4 * cos(x) },
      { 4 * sin(x) },
      { 0 }
    );
    \addplot3+ [accent1, thick, opacity=.2, dashed,
      domain=180:360,
        samples y=0,
        mark=none
    ]
    (
      { 4 * cos(x) },
      { 4 * sin(x) },
      { 0 }
    );
    \addplot3+ [accent2, thick,
      domain=-70:110,
        samples y=0,
        mark=none
    ]
    (
      { (3 + cos(x)) * cos(120) },
      { (3 + cos(x)) * sin(120) },
      { sin(x) }
    );
    \addplot3+ [accent2, thick,
      domain=-70:30,
        samples y=0,
        mark=none,
        -{stealth}
    ]
    (
      { (3 + cos(x)) * cos(120) },
      { (3 + cos(x)) * sin(120) },
      { sin(x) }
    );
    \addplot3+ [accent2, thick,
      domain=-70:34,
        samples y=0,
        mark=none,
        -{stealth}
    ]
    (
      { (3 + cos(x)) * cos(120) },
      { (3 + cos(x)) * sin(120) },
      { sin(x) }
    );
    \addplot3+ [accent2, thick, opacity=.1, dashed,
      domain=110:290,
        samples y=0,
        mark=none
    ]
    (
      { (3 + cos(x)) * cos(120) },
      { (3 + cos(x)) * sin(120) },
      { sin(x) }
    );
  \end{axis}
\end{tikzpicture}

%%FIG intersections_on_torus (Poincare_Duality-6.svg)
\begin{tikzpicture}[>={Stealth[length=6pt,width=5pt]}]
  \draw[name path=line, postaction={decorate}, decoration={markings, mark=at position .56 with {\arrow{>}}}] (0,0) -- (7,0);
  \draw[name path=arc, postaction={decorate}, decoration={markings, mark=at position .5 with {\arrow{>}}}] (0.3,-0.55) .. controls (2,1.15) and (5,1.15) .. (6.7,-0.55);
  \path[name intersections={of=line and arc}];
  \fill[accent1] (intersection-1) circle (2.6pt);
  \fill[accent2] (intersection-2) circle (2.6pt);
\end{tikzpicture}

\end{document}
